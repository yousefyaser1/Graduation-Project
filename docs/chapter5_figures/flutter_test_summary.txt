flutter test  —  summary (run 2026-06-13, after fix)
====================================================
Total: 23 tests   |   PASSED: 23   |   FAILED: 0   ("All tests passed!")

The 5 previously-failing body_part_render_test.dart golden tests were rewritten
as behavioural widget tests (hotspot selection, single-selection replacement,
front/back chevron toggle) using stable widget keys instead of pixel snapshots,
so the suite is now green and robust across machines.

PASSED — logic / security / model unit tests
---------------------------------------------
appointment_test.dart        4/4   Appointment model: toMap/fromMap round-trip;
                                   default status "Scheduled"; copyWith isolation;
                                   missing-field tolerance.
password_hasher_test.dart    6/6   PBKDF2-SHA256 format; verify correct/incorrect;
                                   random-salt uniqueness; legacy-plaintext
                                   detection; malformed-hash rejection.
progression_utils_test.dart  5/5   groupScansByBodyPart grouping + chronological
                                   sort; Unspecified bucket; trackableBodyParts
                                   ordering/filtering; confidenceTrend mapping.
widget_test.dart             2/2   User model round-trip; fromMap defaults.
body_part_render_test.dart   6/6   BodyPartSelectionScreen behaviour: header +
                                   empty-state hint; hotspot tap selects a part;
                                   re-tap clears it; second selection replaces the
                                   first (single-selection); chevron switches to
                                   the back figure and its hotspots are tappable;
                                   a front selection is not highlighted on the back.
